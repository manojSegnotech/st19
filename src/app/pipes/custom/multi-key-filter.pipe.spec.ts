import { MultiKeyFilterPipe } from './multi-key-filter.pipe';

describe('MultiKeyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiKeyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
